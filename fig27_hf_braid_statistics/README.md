# Fig. 27: braid statistics of the single-determinant states

Delta Theta / 2 pi as a function of the separation d/lstar for the single-determinant two-QP and two-QH states of Sec. VII, alpha = 2/7, 2/5, 2/3, k1 = 0, k2 = 0, 1, 2. Exact (overlaps of the determinant states along the braid loop, Mmax = 140).

Files: `hf_braid_statistics_qp.csv`, `hf_braid_statistics_qh.csv`.

Columns: `d_over_lstar`, then `dTheta_over_2pi_alpha{0p2857,0p4,0p6667}_k{0,1,2}`.

Script: `scripts/plot_hf_braid_statistics.jl` (repository `two_anyon_hf`).
